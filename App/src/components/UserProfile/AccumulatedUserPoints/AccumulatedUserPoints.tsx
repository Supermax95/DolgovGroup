import { View, Text } from 'react-native';
import React, { FC } from 'react';

const AccumulatedUserPoints: FC = () => {
  return (
    <View>
      <Text>AccumulatedUserPoints</Text>
    </View>
  );
};

export default AccumulatedUserPoints;
