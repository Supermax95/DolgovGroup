import { View, Text } from 'react-native';
import React from 'react';

const AccumulatedUserPoints = (): JSX.Element => {
  return (
    <View>
      <Text>AccumulatedUserPoints</Text>
    </View>
  );
};

export default AccumulatedUserPoints;
